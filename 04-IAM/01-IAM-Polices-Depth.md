

- IAM Polices
    - Policy language version
    - Policy identifier
    - one or more statments
    - statement id means identifiers
    - where statement allow or deny
    - principle : account user role
    - action to perform
    - resouces
    - conditions

- In Allow action you can add Not action means you allow everything but not allow in action
    - notaction : IAM:*
- In effect Deny if we add Not action means not allow all the services except only not action

